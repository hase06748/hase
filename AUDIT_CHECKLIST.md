# قائمة فحص محسّن الصور

آخر تحديث: v1.1.0 (build8, APK 230.4MB).

## ✅ أُصلح في v1.1.0

| المعرّف | المشكلة | الإصلاح |
|---|---|---|
| **NEW-1** | تشغيل على CPU بدل GPU/NPU | تبديل الحزمة إلى `onnxruntime-android-qnn` — تضم `libQnnHtp.so` (Hexagon) و`libQnnGpu.so` (Adreno) و`libQnnHtpV79Skel.so` المطابق لـ Snapdragon 8 Elite |
| **NEW-2** | «NNAPI» معروض بلا دليل | المحرّك يجرّب 5 مسارات ويقيس زمن بلاطة حقيقية لكلٍّ منها ويحتفظ بالأسرع فقط، ويحفظ الفائز. يرفض أي مسار يُخرج NaN |
| **NEW-3** | معالجة استغرقت ساعة | كل الأنوية بدل 6 · تخطٍّ مبكر عند < 400 مللي/بلاطة · تقليل رسائل التقدّم إلى 150 مللي |
| **NEW-4** | حدة منخفضة | قناع Unsharp بأربع درجات (ناعمة/متوسطة/عالية/قصوى)، الافتراضي «عالية» · المعاينة رُفعت من 3 ميجابكسل/j90 إلى 8 ميجابكسل/j96 |
| **NEW-5** | لا حماية حرارية | `ThermalGovernor` — يقرأ `getCurrentThermalStatus` و`getThermalHeadroom(20s)` ويُدخل دورة تهدئة نسبية بين البلاطات، مع عرض الحالة حيًا |
| **NEW-6** | لا خطة معالجة | `SrEngine.plan()` يحسب الشبكة والزمن والذاكرة قبل البدء ويعرضها |
| **C1** | OOM في وضع «أقصى» | تركيب شريطي: نافذة 256 صفًا فقط + كتابة مباشرة في البِتماب → الذروة ثابتة ~60MB |
| **C2** | لا حماية من التزامن | `AtomicBoolean busy` يرفض أي طلب متزامن بخطأ `BUSY` |
| **C3** | تدوير بِتماب قيد القراءة | `recycleIfUnused` + الحارس يضمن عدم وجود مهمة نشطة |
| **H3** | فشل المشاركة | إضافة `ClipData.newRawUri` مع منح الصلاحية |
| **H5** | تراكم ملفات المشاركة | حذف تلقائي لما يتجاوز 24 ساعة |
| **M1** | فشل HEIF بعمق 10-بت | `setTargetColorSpace` داخل `try/catch` |
| **M5** | تسريب `scope` | `onDestroy` يلغي المهام ويحرّر كل البِتماب ويغلق الجلسة |
| **M6** | `pendingSource` ثابت | حُوّل إلى حقل نسخة |
| **G1** | إغراق خيط الواجهة | خنق الرسائل إلى 150 مللي |
| — | الشاشة تُطفأ فتتوقف المعالجة | `PARTIAL_WAKE_LOCK` طوال التشغيل |
| — | مسار قد يُعلّق الإقلاع | تسجيل «قيد الاختبار» في التفضيلات؛ أي مسار عُلّق يُحظر تلقائيًا في الإقلاع التالي |

---

## 🔴 حرج — يجب إصلاحه أولًا

### ~~C1. نفاد الذاكرة (OOM) في وضع «أقصى جودة»~~ ✅ أُصلح
**الملف:** `SrEngine.kt` (تركيب شريطي)
`acc` + `wsum` + `outPixels` مصفوفات Java على الـ heap بحجم كامل الخرج.

| الوضع | الدخل | الخرج | Java heap | النتيجة |
|---|---|---|---|---|
| سريع | 0.3 MP | 4.8 MP | ~95 MB | ✅ آمن |
| متوازن | 0.8 MP | 12.8 MP | ~253 MB | ⚠️ على الحد |
| **أقصى** | **2.0 MP** | **32 MP** | **~633 MB** | ❌ **OutOfMemoryError مؤكد** |

**الإصلاح:** معالجة على شكل شرائح أفقية (band-by-band) — أبقِ `acc`/`wsum` لصفّ بلاطات واحد فقط (`ow × 256`)، واكتب البكسلات النهائية مباشرةً في `Bitmap` عبر `setPixels` لكل شريط. يخفض الذروة إلى < 60 MB لأي مقاس.
**التحقق:** صورة 2 MP في وضع أقصى → لا تعطّل + مراقبة `Debug.getNativeHeapAllocatedSize()`.

### C2. لا يوجد حماية من التشغيل المتزامن
**الملف:** `MainActivity.kt:99-144`
نداءان لـ `enhance` أو `loadImage` أثناء المعالجة يعملان بالتوازي على نفس `pendingSource` → تلف نتيجة أو OOM مضاعف.
**الإصلاح:** `AtomicBoolean busy` — أرجِع خطأ `BUSY` فورًا. وامنع `recycle()` لبِتماب قيد الاستخدام.
**التحقق:** ضغط سريع متكرر على «تحسين» ثم «اختر صورة» أثناء العمل.

### C3. `recycle()` على بِتماب قيد القراءة
**الملف:** `MainActivity.kt:72-74` + `SrEngine.kt:139`
اختيار صورة جديدة أثناء المعالجة يُعيد تدوير `pendingSource` بينما `src.getPixels()` يقرأه → `IllegalStateException: Can't call getPixels() on a recycled bitmap`.
**الإصلاح:** عدّاد مراجع أو منع الاستبدال أثناء `busy` (مرتبط بـ C2).

---

## 🟠 مهم — يؤثر على قابلية الاستخدام

### H1. زمن المعالجة غير مقبول
عدد البلاطات: سريع ≈154 · متوازن ≈386 · أقصى ≈929.
HAT 20.99M بارامتر لكل بلاطة 64→256.
**الفحص المطلوب:** قِس الزمن الفعلي لبلاطة واحدة على Snapdragon 8 Elite، ثم:
- إن كان > 200 ms/بلاطة → أعِد تصدير ONNX ببلاطة 128 أو 192 (بلاطات أقل بكثير، نفس الجودة).
- فعّل `enableCPUMemArena` + جرّب XNNPACK EP.
**التحقق:** سجّل ms/بلاطة في `deviceInfo` واعرضه في الواجهة.

### H2. NNAPI قد يكون وهميًا على Android 16
**الملف:** `SrEngine.kt:88-100`
`addNnapi()` ينجح في إنشاء الجلسة لكن NNAPI مهجور من Android 15، وقد يُسند كل العُقد للـ CPU بصمت → الواجهة تعرض «NNAPI» كذبًا.
**الإصلاح:** قِس زمن أول بلاطة مع/بدون NNAPI واختر الأسرع فعليًا؛ اعرض الاسم الحقيقي. وفكّر في QNN EP (Hexagon) بدلًا منه.
**التحقق:** قارن الزمن بين الوضعين وسجّله.

### H3. مشاركة الصورة قد تفشل على بعض المشغّلات
**الملف:** `MainActivity.kt:188-194`
`EXTRA_STREAM` بدون `clipData` — بعض التطبيقات (واتساب/تيليجرام على MIUI) لا تحصل على صلاحية القراءة.
**الإصلاح:**
```kotlin
clipData = ClipData.newRawUri("", uri)
addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION)
```
**التحقق:** شارِك إلى واتساب + جيميل + Google Photos.

### H4. PNG بحجم 32 ميجابكسل
**الملف:** `ImageIo.kt:98-106, 109`
ضغط PNG لصورة 32 MP قد يستغرق دقيقة ويُنتج ملفًا > 100 MB، بدون أي مؤشر تقدّم، على الخيط الخلفي بلا مهرب.
**الإصلاح:** حدّ أقصى لحجم PNG، أو تحويل تلقائي إلى JPEG q=95 فوق حدّ معيّن مع تنبيه المستخدم + مؤشر «جارٍ الحفظ».
**التحقق:** احفظ خرج 32 MP بصيغة PNG وقِس الزمن والحجم.

### H5. مجلد `cache/share` لا يُنظَّف أبدًا
**الملف:** `ImageIo.kt:149-155`
كل مشاركة تُخلّف ملفًا كامل الحجم.
**الإصلاح:** احذف الملفات الأقدم من 24 ساعة عند بدء التطبيق.

---

## 🟡 متوسط — سلامة الدخل والصور

### M1. صور HEIF بعمق 10-بت / HDR (كاميرا شاومي)
**الملف:** `ImageIo.kt:40-44`
`setTargetColorSpace(SRGB)` قد يرمي استثناءً على HEIF بعمق 10-بت أو gain map.
**الإصلاح:** غلّف `setTargetColorSpace` في `try/catch`، وفعّل `decoder.setAllocator(SOFTWARE)` دائمًا.
**التحقق:** صوّر بـ Xiaomi 15 Ultra بصيغة HEIF (مع تشغيل HDR) وحاول الفتح.

### M2. مرشّح `FileType.custom` قد يُخفي ملفات HEIC
**الملف:** `home_page.dart:119-125`
اعتماد `allowedExtensions` يُترجم إلى MIME؛ بعض منتقيات MIUI لا تُظهر `.heic`.
**الإصلاح:** جرّب `FileType.image` (يقبل `image/*`) والتحقق من الصيغة بعد الاختيار.
**التحقق:** افتح المنتقي وتأكد من ظهور ملفات `.heic` و`.webp`.

### M3. صور صغيرة جدًا / نسب متطرفة
`originList` مع `size <= TILE` تُرجع `[0]` والحشو بتكرار الحافة → صورة 20×3000 تُحشى إلى 64×3000 (حشو 68%، هدر حسابي + احتمال هالة على الحافة).
**التحقق:** جرّب 16×16 · 20×3000 · 1×1 · صورة بقناة ألفا شفافة.

### M4. قناة الألفا تُفقد بصمت
`SrEngine` يقرأ RGB فقط ويُخرج `Color.rgb` معتم. PNG شفاف يعود بخلفية سوداء/عشوائية.
**الإصلاح:** إما تمرير الألفا عبر تكبير Bilinear ودمجها، أو تنبيه المستخدم.
**التحقق:** PNG بخلفية شفافة.

### M5. `scope` لا يُلغى في `onDestroy`
**الملف:** `MainActivity.kt:23`
تسريب Activity + `runOnUiThread` على activity مُدمَّرة.
**الإصلاح:** `override fun onDestroy() { scope.cancel(); ... }`

### M6. `pendingSource` حقل static
**الملف:** `MainActivity.kt:224`
يُبقي البِتماب حيًا بعد إغلاق الشاشة.
**الإصلاح:** انقله إلى حقل نسخة + `onDestroy` ينظّف.

---

## 🟢 تحسينات

- **G1.** `runOnUiThread` لكل بلاطة (حتى 929 رسالة) — اخنقه إلى مرة كل 100 ms.
- **G2.** حجم APK 208 MB — أعِد محاولة fp16 (فشلت سابقًا بـ NaN بسبب طبقات الانتباه) عبر تثبيت `LayerNorm`/`Softmax` على fp32، أو استخدم `int8` مع معايرة. الهدف < 90 MB.
- **G3.** خرج > 100 MB لا يمكن رسمه على Canvas — تأكد أن المعاينة تستخدم النسخة المصغّرة فقط (حاليًا نعم: 3 MP).
- **G4.** لا يوجد حفظ لحالة المعالجة — تدوير/تصغير التطبيق أثناء العمل يفقد كل شيء. أضِف Foreground Service.
- **G5.** لا توجد أذونات وقت التشغيل مطلوبة فعليًا (SAF) — احذف `permission_handler` إن لم يُستخدم.

---

## 📋 سيناريوهات الفحص اليدوي على الجهاز

| # | السيناريو | المتوقع |
|---|---|---|
| 1 | تثبيت + أول إطلاق | يعرض «CPU» أو «NNAPI»، لا انهيار |
| 2 | JPG 1MP · وضع سريع | ينتهي، مقارنة قبل/بعد تعمل |
| 3 | PNG · وضع متوازن | لا OOM |
| 4 | **أي صورة · وضع أقصى** | ✅ يجب أن ينجح الآن (C1 أُصلح) |
| 4ب | **قراءة اسم المعالج عند أول إقلاع** | «Hexagon NPU» أو «Adreno GPU» — إن ظهر «CPU» فالمسارات الأخرى فشلت القياس |
| 4ج | **تشغيل طويل + مراقبة الحرارة** | تظهر شارة الحرارة وتتحول للبرتقالي مع تهدئة تلقائية |
| 4د | **مقارنة درجات الحدة الأربع** | فرق واضح بين «ناعمة» و«قصوى» بلا هالات |
| 5 | HEIC من الكاميرا | يُفتح ويُعالج (M1) |
| 6 | WEBP | يُفتح |
| 7 | صورة بدوران EXIF 90° | الاتجاه صحيح |
| 8 | إلغاء في منتصف المعالجة | يعود لحالة «جاهز» فورًا |
| 9 | إلغاء ثم تحسين مرة أخرى | يعمل بشكل صحيح |
| 10 | حفظ PNG → المعرض | يظهر في Pictures/PhotoEnhancer |
| 11 | حفظ JPG → المعرض | يظهر |
| 12 | مشاركة إلى واتساب | تصل الصورة كاملة (H3) |
| 13 | تصغير التطبيق أثناء المعالجة | لا انهيار عند العودة |
| 14 | صورتان متتاليتان بدون إعادة تشغيل | لا تراكم ذاكرة |
| 15 | ضغط متكرر سريع على «تحسين» | لا تشغيل مزدوج (C2) |
| 16 | تكبير 8× في المقارنة | لا خطوط بين البلاطات |
| 17 | مقارنة الجودة مع Bicubic ×4 | تفاصيل أحدّ بوضوح |
| 18 | وضع الطيران | يعمل بالكامل (محلي) |

---

## ✅ ما تم التحقق منه بالفعل

- أوزان `Real_HAT_GAN_SRx4_sharper` تُحمَّل بـ `strict=True`
- ONNX مطابق لـ PyTorch (أقصى فرق 2e-5)
- `onnxslim` مطابق بت-ببت للتصدير الأصلي
- fp16 مرفوض (خرج NaN 100%) → fp32 نهائي
- تراكب 8px هو الأفضل (35.5 dB مقابل 29.0 لـ 4px و31.5 لـ 12px)
- لا خطوط بلاطات (تدرّج الحدّ 0.0048 < متوسط الصورة 0.0097)
- APK: arm64-v8a فقط · النموذج 165,066,073 بايت · موقّع دائمًا
- `flutter analyze` نظيف · `flutter test` ناجح
- واجهة RTL عربية سليمة (تحقّق بصري بحجم 800×1200)

---

# 📦 الإصدار 1.2.0 — البنية السداسية المراحل

## البنية الجديدة

| # | المرحلة | الملف | النموذج | الحجم |
|---|---------|-------|---------|-------|
| 1 | تحليل الجودة والتلف | `QualityAnalyzer.kt` + `FaceDetector.kt` | `yunet.onnx` | 0.23 MB |
| 2 | التنظيف (تشويش + JPEG + ضبابية) | `CleanupStage.kt` | `scunet_fp16.onnx` | 40 MB |
| 3 | التكبير ×4 | `SrEngine.kt` | `hat_x4.onnx` (fp32) | 165 MB |
| 4 | معالجة المناطق الذكية | `FaceStage.kt` | `gfpgan.onnx` (fp16) | 163 MB |
| 5 | دمج التفاصيل والحدة التكيّفية | `DetailFusion.kt` | — (حسابي) | — |
| 6 | بوّابة الجودة | `QualityGate.kt` | `sface.onnx` | 39 MB |

المنسّق: `Pipeline.kt` — يحمّل نموذج كل مرحلة ويحرّره فورًا، فلا يجتمع
HAT + GFPGAN + SCUNet في الذاكرة أبدًا.

## قرارات هندسية موثّقة بالقياس

- **QNN أُلغي**: خلفياته Hexagon/HTP و Adreno حصرية لـ Qualcomm. على
  Dimensity 9400+ كانت تضيف ~70 MB مكتبات لا تُحمَّل أبدًا وكل فحص يسقط
  إلى CPU صامتًا. البديل: `onnxruntime-android` القياسي + NNAPI الذي يصل
  إلى APU 890 فعليًا.
- **HAT يبقى fp32**: تتبّع 170 عقدة أثبت أن التنشيطات تتصاعد حتى 23168
  عند `layers.5` وتتجاوز سقف fp16 (65504) → ∞ → NaN عند العقدة 3036.
  محاولة حجب LayerNorm/Softmax/Erf/Div لم تنفع. الجودة المطلقة ⇒ fp32.
- **SCUNet مُختار بالقياس**: +4.47 dB على تشويش σ18 + ضغط JPEG q30
  (26.89 → 31.36 dB)، ويغطي التشويش وآثار JPEG بنموذج واحد.
- **NAFNet مرفوض**: يعمل عند 512×512 فقط (7.6 ث/بلاطة) و fp16 يعطي NaN.
  البديل: حدّة اتجاهية حسابية مبوّبة بقناع الحواف داخل `CleanupStage`.
- **YuNet بـ BGR** لا RGB: الدرجات أعلى قياسًا (0.92 / 0.91 على وجوه حقيقية).

## فحوصات الإصدار 1.2.0

| # | الفحص | المتوقع |
|---|-------|---------|
| 19 | اسم الخلفية على Dimensity 9400+ | «مسرّع NNAPI» لا «المعالج» |
| 20 | صورة نظيفة بلا وجوه | يتخطى المرحلتين 2 و4 تلقائيًا |
| 21 | صورة JPEG مضغوطة بشدة | تظهر «آثار JPEG» في التحليل وتُنظَّف |
| 22 | صورة بوجه واحد | «ترميم الوجوه» يعمل وتطابق الهوية ≥ 55% |
| 23 | صورة بسماء زرقاء واسعة | لا تحبّب في السماء (حماية السماء) |
| 24 | صورة ببشرة قريبة | لا خشونة في البشرة (تخميد ×0.42) |
| 25 | إيقاف «التنظيف» يدويًا | تُتخطّى المرحلة 2 ويظهر ذلك في التقرير |
| 26 | إيقاف «فحص الجودة» | لا يظهر سطر «أُعيدت … منطقة» |
| 27 | إلغاء أثناء مرحلة التحليل | يُلغى فورًا (العلم انتقل إلى Pipeline) |
| 28 | شريط المراحل الستة | النقطة النشطة تتقدّم ولا تعود للخلف |
| 29 | الزمن المتبقي | يظهر فقط في مرحلتَي التنظيف والتكبير |
| 30 | صورة بحركة اهتزاز | يُصنَّف MOTION والحدة عمودية على اتجاه اللطخة |

## ✅ ما تم التحقق منه في 1.2.0

- بناء APK ناجح: **437.6 MB** · arm64-v8a فقط · موقّع (APK Sig Block 42)
- المكتبات الأصلية: `libonnxruntime.so` القياسي فقط — **صفر ملفات QNN**
- النماذج الخمسة مضمّنة ومطابقة الأحجام بايت-ببايت
- `com.photoenhancer.editor` · minSdk 28 · targetSdk 36
- `flutter analyze` نظيف · تجميع Kotlin ناجح لكل الملفات الاثني عشر

---

# 🔬 الإصدار 1.2.1 — فحص المحرك ومراجعة الأنبوب

## أولًا: صحة محرك الاستدلال — **الخمسة سليمة**

قياس فعلي بتشغيل كل نموذج على صور حقيقية، لا مراجعة كود:

| النموذج | الزمن | المدى | NaN/Inf | النتيجة |
|---|---|---|---|---|
| HAT ×4 | 3129–4476 ms/مربّع | [0.00, 1.04] | 0 / 0 | تفاصيل **1.40×** فوق bicubic |
| SCUNet | 805–3001 ms/مربّع | سليم | 0 / 0 | **+4.56 dB** · نسبة حدود 1.106 (بلا خطوط) |
| GFPGAN | 2441 ms | [-1.125, +1.220] | 0 / 0 | تفاصيل **2.15×** |
| SFace | سريع | — | 0 | تشابه الهوية **0.9556** (الحد 0.55) |
| YuNet | سريع | — | 0 | ثقة 0.913 / 0.917 · النقاط على العين والأنف والفم |

- كل حدود الدخل/الخرج **fp32** ومطابقة لما يغذّيه Kotlin.
- قيم HAT كلها داخل نطاق القبول `[-0.5, 1.5]` → ماسح NaN لن يرفض خطأً.
- تجزيء SCUNet في Kotlin أُعيد إنتاجه بالضبط: 240×240→256×256 · 80×240→128×256 · 80×80→128×128.
- عقد الأبعاد **سليم**: CleanupStage يحفظ الأبعاد، وSrEngine يقتصّ إلى `sw*4 × sh*4`، لذا فهرسة `box × 4` في المرحلة 4 صحيحة.

## ثانيًا: مراجعة الأنبوب — ثلاث مشاكل، وقد عُولجت

### العلّة 1 — المحلّل لم يكن يرى البكسل الأصلي أبدًا *(عالية)*

`MainActivity` كان ينادي `downscaleIfNeeded` قبل `Pipeline.run`، وهو تصغير
**ناعم** يمرّر الصورة عبر مرشّح ترددات منخفضة — أي يمحو بالضبط الحبيبات التي
تقيسها المرحلة 1. على صورة 12 ميجابكسل σ18 + JPEG q30:

| دخل التحليل | sigma | يُنظّف؟ |
|---|---|---|
| 4000×3000 أصلية | 7.50 | **نعم** |
| سريع → 632×474 | 2.32 | **لا** |
| متوازن → 1032×774 | 2.80 | **لا** |
| أقصى → 1632×1224 | 4.01 | نعم |

أي أن صورة مشوّشة فعلًا تُصنّف سليمة على أكثر النمطين استخدامًا، فلا تعمل
المرحلة 2 إطلاقًا.

**المعالجة:** `Pipeline.run` صار يستقبل `native` (النسخة الأصلية قبل التصغير)
ويمرّرها إلى `QualityAnalyzer.analyze`. الوجوه تُستخرج من الصورة العاملة
فتبقى إحداثياتها في الفضاء الذي تستخدمه المرحلتان 4 و6.

### العلّة 2 — كشف JPEG كان معطوبًا بنيويًا *(عالية)*

خطوة الاختزال الصحيح لصورة 4000×3000 هي **7**، و`8 % 7 = 1`، فتتحطّم شبكة
DCT ذات الـ8 بكسل التي يعتمد عليها المقياس بالكامل. القياس على q30:

| الطريقة | النسبة | الدرجة | الحكم |
|---|---|---|---|
| مستوى مختزل (السابق) | 1.0490 | 0.082 | **فوّتها** |
| قطعة أصلية 512×512 محاذية | 2.0443 | 1.000 | **صحيح** |
| صورة نظيفة (ضابط) | 0.9986 | 0.000 | لا إنذار كاذب |

المشكلة مستقلة عن العلّة 1 — تفشل حتى بالدقة الكاملة.

**المعالجة:** `nativeWindows()` تلتقط حتى 9 نوافذ 256×256 بالدقة الأصلية،
كل واحدة تبدأ على مضاعف 8 فتبقى شبكة الضاغط في موضعها، ثم يُؤخذ الوسيط.

التحقّق بعد الإصلاح:

| الحالة | الدرجة | يُزال التكتّل؟ |
|---|---|---|
| نظيفة | 0.000 | لا |
| q90 (خفيف) | 0.315 | نعم |
| q60 | 1.000 | نعم |
| q30 | 1.000 | نعم |
| q15 | 1.000 | نعم |

والتشويش أيضًا صار يتبع الحقيقة: σ حقيقي 0→0.19 · 3→2.25 · 6→4.43 ·
10→7.43 · 18→13.22 · 28→20.26 — وزال القياس الكاذب 1.14 عند σ=0.

### العلّة 3 — مرجع البوابة كان الصورة الخطأ *(منطقية — والقياس لم يؤكّد ضررها)*

البوابة كانت تقارن الخرج بـ`source` الأصلية، بينما المكبّر رأى الصورة
**المنظّفة**. طاقة التفاصيل المرجعية: bicubic للمشوّشة 5.077 مقابل
المنظّفة 2.908، فعتبة النجاح 4.468 أمام المشوّشة و2.559 أمام المنظّفة —
أي أن خرجًا منظّفًا صحيحًا (~2.91) يسقط تحت عتبة المشوّشة. الفرق السلوكي:
البوابة بالمرجع المشوّش أرجعت **11 من 25** خلية، وبالمرجع المنظّف **صفر**.

**بأمانة: فرضيتي الأولى لم تصدق.** توقّعت أن ذلك يُعيد حقن التشويش، لكن
PSNR مقابل الحقيقة كان: بلا بوابة 27.52 · المرجع المشوّش 27.62 · المرجع
المنظّف 27.52 — أي أن الخطأ كان **أفضل بقليل**. وبقايا المناطق المستوية:
حقيقة 0.882 · بلا بوابة 2.238 · المشوّش **2.088** · المنظّف 2.238.

فالعيب الحقيقي ليس تدهور جودة، بل أن القرار **غير مبدئي**: البوابة كانت
تُرجع خلايا لأنها تحمل طاقة تشويش أقل من الدخل، وهو ما أزاله الأنبوب
عن قصد. سلوك غير متوقّع، لا سلوك ضار.

**المعالجة:** `working` (ما رآه المكبّر فعلًا) لم يُعد يُحرَّر بعد المرحلة 3،
بل يُمرَّر إلى البوابة ثم يُحرَّر قبل العودة. كل مسارات الخروج الخمسة
تُحرّره أيضًا حتى لا تتسرّب الذاكرة.

## اختبارات 31–36

| # | الاختبار | النتيجة |
|---|---|---|
| 31 | HAT بلا NaN وداخل نطاق القبول | ✅ |
| 32 | تجزيء SCUNet مطابق لـKotlin بايت-ببايت | ✅ |
| 33 | SFace لا يرجع ترميمًا سليمًا (0.9556) | ✅ |
| 34 | التشويش يُقاس بالدقة الأصلية | ✅ |
| 35 | كشف JPEG يميّز q90..q15 بلا إنذار كاذب | ✅ |
| 36 | البوابة تقارن بما رآه المكبّر | ✅ |

## التحقّق النهائي

- APK: **437.6 MB** (437,629,090 بايت) · بناء 87.8 ثانية
- **صفر ملفات QNN/Hexagon/HTP** · `libonnxruntime.so` القياسي فقط
- ABI: `arm64-v8a` فقط · موقّع (APK Sig Block 42)
- النماذج الخمسة مضمّنة بأحجامها الأصلية
- `com.photoenhancer.editor` · minSdk 28 · targetSdk 36 · compileSdk 36

---

# ⚙️ الإصدار 1.3.0 — المسرّعات ومطابقة الألوان وصدق التكبير

## 1. سلّم المسرّعات لكل النماذج الخمسة

كانت المشكلة الحقيقية: **أربعة من الخمسة كانت تعمل على المعالج فقط**.
`SrEngine` وحده كان له سلّم مسرّعات؛ أما SCUNet وGFPGAN وYuNet وSFace فكانت
`SessionOptions()` عادية بلا أي مسرّع. `Accelerator.kt` الجديد يوحّدها:

| النموذج | التسمية | الدقّة | السبب |
|---|---|---|---|
| HAT | `SR` | STRICT | fp16 غير آمن — التنشيطات تصل 23168 |
| SCUNet | `تنظيف` | RELAXED | مزيل تشويش، خرجه تقدير مُنعّم أصلًا |
| GFPGAN | `وجوه` | STRICT | مولّد يرسم وجه إنسان |
| YuNet | `كشف` | RELAXED | إحداثيات تقريبية + عتبة ثقة وNMS بعدها |
| SFace | `هوية` | RELAXED | تشابه جيبي مقابل عتبة 0.55، هامش واسع |

السلّم: **NNAPI fp32 → (fp16) → XNNPACK → CPU**. كل درجة مسرّعة تُختبر
بـ`verify` على بيانات فعلية قبل قبولها، لأن السوّاقة قد تقبل الرسم ثم تُرجع NaN.

### لماذا لا QNN ولا TFLite GPU

- **QNN** حصري لعتاد Qualcomm. الجهاز الهدف **MediaTek Dimensity 9400+**، فـ
  `libQnnHtp.so` لا يمكن أن يُحمَّل عليه إطلاقًا — قِيس سابقًا: يفشل في كل
  محاولة ويضيف ~70 ميجابايت. NNAPI هو الباب المحايد إلى **نفس** العتاد
  (APU من MediaTek).
- **TFLite GPU delegate** يخصّ TFLite. هذه رسوم **ONNX** يشغّلها ONNX Runtime؛
  لا يمكن ربط delegate تابع لـTFLite بها تقنيًا. الوصول إلى GPU يمرّ عبر
  NNAPI، وهو الدرجة الأولى بالفعل.

الواجهة تعرض الآن ما يعمل فعلًا لكل نموذج: `المسرّعات: SR:NNAPI · تنظيف:XNNPACK …`

## 2. التراكب والتنعيم بين المربّعات — **موجود مسبقًا**

الطلب كان: overlap 16-32 مع feathering بدل «الحواف الحادة الحالية».
لا توجد حواف حادة — `SrEngine` يستخدم **نافذة Hann ثنائية الأبعاد** (`blendWindow`)
مع تجميع مرجّح وتطبيع بـ`wsum`، وهو تنعيم أنعم من linear blend.

ورفع overlap إلى 32 **مستحيل رياضيًا**: `STRIDE = TILE - 2*OVERLAP`، و
`TILE = 64` مثبّت في الرسم (اختُبر: 128×128 يُرفض بـINVALID_ARGUMENT)،
فـ`64 - 2*32 = 0` أي خطوة صفرية = حلقة لا نهائية. والتكلفة:

| overlap | stride | مربّعات (أقصى) | مقابل الحالي |
|---|---|---|---|
| **8 (الحالي)** | 48 | **3,468** | — |
| 16 | 32 | 7,752 | **2.24×** |
| 24 | 16 | 30,502 | **8.79×** |
| 32 | 0 | مستحيل | — |

بمعدّل 3.5 ثانية للمربّع، overlap=16 يرفع الزمن من ~3.4 ساعة إلى ~7.5 ساعة
مقابل صفر تحسين بصري (لا حواف أصلًا). **لم يُطبّق، والسبب مقيس.**

## 3. مطابقة الهيستوغرام لمخرجات GFPGAN — **مُطبّق**

`matchTone()` في `FaceStage.kt`: مطابقة هيستوغرام لكل قناة بعكس الـCDF،
والإحصاءات تُجمع **داخل 0.52 من القطع الناقص فقط** (الجزء الذي سيُدمج بوزن
كامل)، لأن أخذ الزوايا يشوّه الخريطة ببكسلات ستُطرح أصلًا.

**لماذا مطابقة هيستوغرام لا Poisson blending:** Poisson يحلّ حقل تدرّج مع
تثبيت **الحدّ** على الخلفية — وهو الأداة الصحيحة عند لصق رقعة معتمة بحدّ حادّ.
هنا لا يوجد حدّ: `blend()` يدمج عبر ألفا قطع-ناقص بجيب-مرتفع مع تخميد للبشرة
وحافز للمعالم، أي ألفا متصلة أصلًا. الخلل الفعلي **عام** لا حدودي: طيف FFHQ
مقابل طيف هذه الصورة. مطابقة التوزيع تعالج السبب مباشرة في مرور واحد.

**تصحيح لنفسي:** بنيت أولًا نسخة بنصف القوة معتقدًا أن المطابقة الكاملة
ستُرجع نعومة الأصل فوق الترميم. القياس كذّب ذلك — إزاحة GFPGAN هي **ضغط
تباين**، فعكسها كاملًا يُعيد التباين لا يُزيله:

| النسخة | متوسط | انحراف | قفزة عند الحدّ | طاقة التفاصيل |
|---|---|---|---|---|
| الهدف (الأصل) | 117.43 | 26.12 | — | — |
| GFPGAN خام | 146.07 | 18.81 | **28.59** | 1.000× |
| نصف القوة | 131.64 | 22.47 | 14.15 | 1.157× |
| **كامل** | **117.71** | **26.13** | **0.22** | **1.314×** |

الكامل أفضل في **المقياسين**، فحُذف نصف القوة.

## 4. منع التكبير الوهمي واستغلال الذاكرة — **مُطبّق**

هذه كانت ثغرة حقيقية. الأنبوب يصغّر إلى سقف النمط ثم يضرب ×4، فعلى صورة
50 ميجابكسل بنمط «سريع» يُرمى **97.6%** من البكسلات ويُلفّق ×16:

| النمط | الأصل | العمل | الناتج | المُحتفظ | الربح الحقيقي |
|---|---|---|---|---|---|
| سريع | 12MP | 1.20MP | 19.2MP | 10.0% | 1.60× |
| سريع | 50MP | 1.20MP | 19.2MP | 2.4% | **0.38× خسارة** |
| متوازن | 50MP | 3.20MP | 51.2MP | 6.4% | **1.02× بلا ربح** |
| أقصى | 50MP | 8.00MP | 128.0MP | 16.0% | 2.56× |

`MemoryBudget.kt` يعالج الأمرين معًا:
- **يرفع السقف** إلى ما تسمح به الكومة الفعلية (`maxMemory × 0.55`) وثلث RAM
  الجهاز — أيّهما أصغر — بدل رقم ثابت متحفّظ. فذاكرة 12-16 جيجابايت تُستغل.
- **يكشف الوهم**: `isRealGain()` تقارن `working × 16` بالأصل، والواجهة تُظهر
  تحذيرًا برتقاليًا صريحًا قبل إهدار دقائق على ناتج أفقر من الأصل.

عن VRAM: لا توجد ذاكرة فيديو منفصلة تُحاسب هنا — APU وGPU في هذه الفئة
مدمجان ويعنونان نفس DRAM، فميزانية واحدة تغطيهما.

## اختبارات 37–42

| # | الاختبار | النتيجة |
|---|---|---|
| 37 | النماذج الخمسة كلها على سلّم مسرّعات موحّد | ✅ |
| 38 | كل درجة مسرّعة تُختبر بـverify قبل القبول | ✅ |
| 39 | HAT يرفض 128×128 → TILE=64 مثبّت (overlap 32 مستحيل) | ✅ |
| 40 | مطابقة الهيستوغرام: قفزة الحدّ 28.59 → 0.22 | ✅ |
| 41 | كشف التكبير الوهمي: 50MP/سريع = 0.38× → تحذير | ✅ |
| 42 | السقف يُرفع من الكومة وRAM الفعليتين | ✅ |

## التحقّق

- APK: **437.6 ميجابايت** (437,643,310 بايت)
- صفر QNN/Hexagon/HTP · `arm64-v8a` فقط · موقّع · 5 نماذج
- `flutter analyze` نظيف · تجميع Kotlin نظيف من المحاولة الأولى
